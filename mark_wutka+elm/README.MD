# Elm Sticky Note Implementation

To build:
`elm-make stickynotes.elm --output=stickynotes.js`

If you get errors about not finding the Mouse package, install it:
`elm-package install elm-lang/mouse`

